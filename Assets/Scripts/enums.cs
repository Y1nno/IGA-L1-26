using UnityEngine;

public enum IngredientType
{
    Topping,
}

public enum RecipeType
{
    Pizza,
}

public enum StepType
{
    Chopping,
    Cooking,
    Plating
}
public enum StationType
{
    CuttingBoard,
    Stove,
    Plate
}